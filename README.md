# StablePulse

![Version](https://img.shields.io/badge/version-2.0.0-2A6FDB)
![License: Source-Available](https://img.shields.io/badge/license-source--available-blue)
![Tests](https://img.shields.io/badge/tests-70%20passing-green)
![Bundle](https://img.shields.io/badge/bundle-52%20kB%20(17%20kB%20gzip)-orange)
![Node](https://img.shields.io/badge/node-%3E%3D18-339933)

Professional **USDT & USDC intelligence platform**: peg stress monitoring, cross-chain mint/burn flows, whale-watch anomaly detection, and AI-generated market narratives. Standalone frontend edition.

Created by [Keshav Maheshwari](https://www.withkeshav.com)

> **Source-available.** Free for personal / non-commercial use. Modification is **not** permitted. See [LICENSE](./LICENSE).

## Screenshots

> Screenshots to be added here. Placeholder for the dashboard overview, coin detail, and mobile views.

## Features

- **Peg Stress Index**: a 0-100 risk score driven by peg drift, active alerts, and cross-chain flow pressure.
- **Supply & price history**: 90-day charts per stablecoin, updated on a background sync cadence.
- **Cross-chain mint / burn**: ranked per-chain deltas and a migration detector (which chains are bleeding into which).
- **Whale Watch**: z-score based detection of supply anomalies across chains.
- **AI signal**: narrative headline plus per-alert explanations generated by any OpenAI-compatible model (cloud or self-hosted).
- **Alerts hub**: filter by severity / coin, sort by recency / severity / magnitude, group by rule.
- **Multi-coin ready**: the UI is driven entirely by `ACTIVE_STABLECOINS`; adding a coin is a config change.
- **Themes**: Light default, plus Dark and System. Compact mode, auto-refresh with a manual **Refresh now** button.
- **Mobile-first**: bottom tab bar, card-based tables, safe-area aware, charts rescale.

## Tech stack

- [Preact](https://preactjs.com) + [Vite](https://vitejs.dev): main bundle ~52 kB (17 kB gzip).
- [Chart.js](https://www.chartjs.org): lazy-loaded, only Line/Bar registered (~60 kB gzip, loaded on first chart).
- [Vitest](https://vitest.dev): unit tests for the pure logic (`formatters`, `derive`).
- **No backend in this repo.** Data comes from a single pre-aggregated `/api/dashboard` JSON.

## Requirements

- Node.js **18 or newer** (for build tooling). No server required at runtime.

## Quickstart

```bash
npm install
npm run dev       # dev server (uses http://127.0.0.1:8787 by default; point wrangler dev there)
npm run build     # production build -> dist/
npm run preview   # preview the production build
npm test          # run unit tests
```

## How it works

```
Cloudflare Worker (cron background sync)
  |- pulls DefiLlama chain balances + CoinGecko prices/tickers
  |- generates AI narrative (OpenAI-compatible, keys stay server-side)
  +- serves ONE cached JSON payload:  /api/dashboard
                     |
Static frontend (Cloudflare Pages / VPS / anywhere)
  +- fetches /api/dashboard -> derive.js computes display stats client-side
```

The frontend makes **one request per refresh** and computes the stress index, chain flows, migration pairs, and whale-watch rows locally in `src/lib/derive.js`. See [docs/architecture.md](./docs/architecture.md) and [docs/api-protocol.md](./docs/api-protocol.md).

## Configuration

The API base URL resolves in this order:

1. `window.STABLEPULSE_CONFIG.apiBase` (runtime override, set it in `index.html` or before the bundle loads).
2. `import.meta.env.STABLEPULSE_API_BASE` (build-time env var, `STABLEPULSE_API_BASE=... npm run build`).
3. Default: dev uses `http://127.0.0.1:8787`, prod uses the Cloudflare Worker URL.

Example runtime override:

```html
<script>
  window.STABLEPULSE_CONFIG = { apiBase: 'https://my-endpoint.example.com' };
</script>
```

**CORS:** the worker must send `Access-Control-Allow-Origin` covering the origin the site is served from (use `*` or list your Pages/VPS domain). Dev (`localhost`) and prod both need to pass.

**Note:** the UI brand is StablePulse while the default worker subdomain is `stablescope-cors.keshav-maheshwari.workers.dev`. They intentionally do not need to match.

### Settings (persisted to localStorage)

- Theme (`stablepulse:theme`), default Light.
- Auto-refresh interval (`stablepulse:refresh`): 1/3/5/10/15 min.
- Compact mode (`stablepulse:compact`).

## Adding a stablecoin

The frontend is fully registry-driven. Adding a coin is **one config entry** (provided the worker serves its data):

```js
// src/utils/coin-config.js
USDE: {
  symbol: 'USDE',
  coingeckoId: 'ethena-usde',
  llamaStablecoinId: 123,
  color: '#8B5CF6',
  thresholds: { pegCriticalBps: 50, pegWarnBps: 10, chainSpikeUsd: 500e6, megaSupplyUsd: 1e9 },
},
// then add 'USDE' to ACTIVE_STABLECOINS
```

The nav tabs, home stats/charts, chain-flow bars, and whale watch all follow the list automatically. The worker must expose matching payload fields: `usdeDetail` (chain balances), `cgUSDE` / `cgUSDEChart` (Coingecko), and the coin under `allStables.peggedAssets` + `cgSimple`.

## AI configuration

AI runs **entirely in the worker**; keys never reach the browser. The frontend is provider-agnostic and renders whatever the worker returns (`intelligence.headline` / `narrative` / `implications` and `/api/alert-explain`).

Point the worker at any **OpenAI-compatible** endpoint via env vars:

| Env var | Example |
|---|---|
| `OPENAI_BASE_URL` | `https://openrouter.ai/api/v1` |
| `OPENAI_API_KEY` | your key |
| `OPENAI_MODEL` | `deepseek/deepseek-chat` |

- Generate the narrative during the **cron background sync** so users never wait on the model.
- **Cache `/api/alert-explain` results** (e.g. in KV keyed by alert id). Expanding an alert is the only on-demand AI call and should be instant on repeat.
- The UI degrades gracefully if AI is down or absent, so the app keeps working either way.

## Deploying

### Cloudflare Pages (free, recommended) + existing Worker

```bash
npm install
npm run build
npx wrangler pages deploy dist
```

Point `STABLEPULSE_CONFIG.apiBase` at your worker (or build with `STABLEPULSE_API_BASE`). Confirm the worker's CORS header allows the Pages domain.

### VPS (nginx / Caddy)

The frontend is static files with a configurable API URL: deploy `dist/` anywhere and set `apiBase`. The VPS only serves static assets; it never calls upstream APIs, so its IP is never exposed to CoinGecko/DefiLlama rate limits.

## Repository layout

```
.
|- index.html                 entry, pre-paint theme bootstrap, meta, favicon
|- src/
   |- config.js               API base resolution + APP_VERSION
   |- styles.css              single stylesheet (CSS variables, themes, mobile)
   |- main.jsx                Preact mount
   |- App.jsx                 fetch loop, refresh state, tab routing
   |- components/
      |- Header.jsx, Sidebar.jsx, MobileNav.jsx, SettingsPanel.jsx, ThemeToggle.jsx
      |- Tabs/                HomeTab, CoinTab, ChainsTab, AlertsTab
      |- Sections/            SignalHero, MarketPulse, CapitalFlows, WhaleWatch, SignalSummary
      |- ui/                  StatCard, Sparkline, ChartWrapper, RefreshCountdown, SkeletonLoader
      |- Alerts/              AlertHero, AlertCard
   |- hooks/useTheme.js       theme state (default light)
   |- lib/derive.js           pure display logic + tests
   |- utils/                  formatters.js, coin-config.js + tests
|- docs/                      architecture, API protocol, scaling
|- CHANGELOG.md
|- AGENTS.md
|- LICENSE
```

## Documentation

- [docs/architecture.md](./docs/architecture.md): data flow, module map, derive.js, config resolution.
- [docs/api-protocol.md](./docs/api-protocol.md): `/api/dashboard` + `/api/alert-explain` payload contract, CORS.
- [docs/scaling.md](./docs/scaling.md): worker scaling playbook (free-tier budgets).
- [CHANGELOG.md](./CHANGELOG.md): version history.
- [SECURITY.md](./SECURITY.md): how to report vulnerabilities.
- [AGENTS.md](./AGENTS.md): conventions for AI coding assistants.

## License

**StablePulse - Source-Available License.** Free to use, run, and install for personal, non-commercial purposes. Modification and redistribution of modified versions are not permitted; verbatim redistribution with attribution is allowed. Commercial use or modification requires a separate license from the author. See [LICENSE](./LICENSE).
