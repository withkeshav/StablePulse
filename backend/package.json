{
  "name": "stablesense-backend",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "StableSense lite VPS backend: history accumulation (SQLite) + AI narrative service.",
  "scripts": {
    "start": "node server.js",
    "fetch": "node jobs/fetch.js",
    "ai": "node jobs/ai.js",
    "sync": "node jobs/fetch.js && node jobs/ai.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "fastify": "^5.6.2"
  }
}
